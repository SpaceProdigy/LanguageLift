import { Box, styled } from "@mui/material";

export const Wrapper = styled(Box)`
  width: 100%;
  height: 100%;
  margin-top: 30px;
  margin-bottom: 30px;
`;

export const BooksWraper = styled(Box)`
  width: 100%;

  display: flex;
  flex-wrap: wrap;

  gap: 20px;
`;
